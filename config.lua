
-- locale option
Locale = 'en'

-- configurable options

Shared.fields = true

Shared.vendor = true

-- server options
Shared.options = {

    -- enable vanilla dispatch
    dispatch = false,

    -- enable vanilla radios
    radios = false,

    -- enable idle camera
    idle = false,

    -- enable vanilla hud
    hud = false,

    -- enable vanilla reticle
    reticle = false,

    -- adjust water levels 
    water = {
        active = true,
        value = 0.6
    },

    -- enable ped weapon drop
    drop = false,
}